version: '3.9'
services:
  api:
    build: .
    ports: ["5000:5000"]
    depends_on: [postgres, redis]
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: monitor
  redis:
    image: redis:7
